# # # # # # # # # # # # # # # # # # # # #
# Valores del logging de Hibernate
#

# # # # # # # # # # # # # # # # # # # # #
# JDBC
#
jakarta.persistence.jdbc.url=jdbc:mariadb://localhost:3306/java_importacion_opendata_gc
jakarta.persistence.jdbc.driver=org.mariadb.jdbc.Driver
jakarta.persistence.jdbc.user=root
jakarta.persistence.jdbc.password=malaga

# # # # # # # # # # # # # # # # # # # # #
# ESQUEMA POR DEFECTO
#
#hibernate.default_schema=imp_placsp_gc

# # # # # # # # # # # # # # # # # # # # #
# HikariCP
#
hibernate.hikari=jarios
hibernate.hikari.maximumPoolSize=30
hibernate.hikari.minimumIdle=15
# 30 minutos
hibernate.hikari.maxLifetime=1800000
# 15 segundos
hibernate.hikari.leakDetectionThreshold=15000
# 30 segundos
hibernate.hikari.idleTimeout=30000
# 30 segundos
hibernate.hikari.connectionTimeout=30000
# 5 segundos
hibernate.hikari.validationTimeout=5000
# 1 segundo
hibernate.hikari.initializationFailTimeout=1000
# 5 minutos
hibernate.hikari.keepaliveTime=300000
hibernate.hikari.connectionInitSql=SELECT 1
hibernate.hikari.poolName=myAppImportFromGcHikariPool
hibernate.hikari.readOnly=false
hibernate.hikari.isolateInternalQueries=true

# # # # # # # # # # # # # # # # # # # # #
# ESQUEMA. Opciones:
#
#       validate: validate the schema, makes no changes to the database.
#       update: update the schema si detecta cambios.
#       create: creates the schema, destroying previous data.
#       create-drop: drop the schema when the SessionFactory is closed explicitly, typically when the application is stopped.
#       none: does nothing with the schema, makes no changes to the database-->
hibernate.hbm2ddl.auto=create

# # # # # # # # # # # # # # # # # # # # #
# JTA PLATFORM
#
hibernate.transaction.jta.platform=org.hibernate.service.jta.platform.internal.NarayanaJtaPlatform

# # # # # # # # # # # # # # # # # # # # #
# LOGS
#
hibernate.show_sql=false
hibernate.format_sql=false
hibernate.highlight_sql=false

# # # # # # # # # # # # # # # # # # # # #
# 2º LEVEL CACHE
#
#hibernate.cache.use_second_level_cache=true
#hibernate.cache.region.factory_class=org.hibernate.cache.caffeine.CaffeineRegionFactory
#hibernate.cache.use_query_cache=true

# # # # # # # # # # # # # # # # # # # # #
# STATISTICS 2º LEVEL CACHE
#
#hibernate.generata_statistics=false
